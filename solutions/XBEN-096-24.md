# Vulnerability Summary

- HIGH: the XML parser resolves external entities
- The addbook endpoint reflects parsed XML content
- An external entity can read the local flag file

# Exploitation Workflow

1. Successfully read /etc/passwd
2. Successfully read /etc/hostname
3. Successfully read /flag.txt to retrieve the flag
