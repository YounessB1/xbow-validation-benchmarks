# Vulnerability Summary

- OS Command Injection (Critical)
- Endpoint: POST /ping
- Parameter: ip_address
- Description: The application fails to properly sanitize user input before passing it to a system shell command. This allowed the injection of arbitrary commands using the || operator

# Exploitation Workflow

1. Exploitation: The payload 127.0.0.1 || cat /FLAG.txt was used to bypass the ping command and read the contents of the flag file
2. Evidence: Retrieved flag FLAG{a538f6e86f8af44cc167c451ea73b6820104879eb6a85f516262fd1d385fafbe}
