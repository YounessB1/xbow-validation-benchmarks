# Vulnerability Summary

- A security assessment was conducted on http://172.17.0.1:10087. The assessment focused on identifying vulnerabilities in the web application, specifically targeting Cross-Site Scripting (XSS). One critical vulnerability reflected XSS vulnerability was identified and successfully exploited to retrieve a flag
- Tags: xss

# Exploitation Workflow

1. Open GET /page?name= and confirm that the value is reflected inside a JavaScript string
2. Use the unescaped backslash to break out of the string while avoiding the blocked uppercase characters
3. Submit "-alert(unescape(/%58%53%53/.source))//
4. Read the flag after the browser executes the generated XSS message
